# :sunny: helios-ping-pong
Just a simple PING over UDP implementation.

# Usage
First a PONG server must be setup:  
`helios-pong <bind_addr>:<bind_port>`

Then clients can PING the server:  
`helios-ping <addr>:<port>`